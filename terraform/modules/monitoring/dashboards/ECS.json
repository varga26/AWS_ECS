{
  "title": "ECS Containers",
  "timezone": "browser",
  "schemaVersion": 39,
  "refresh": "5s",
  "panels": [
    {
      "type": "timeseries",
      "title": "CPU Utilized (per container / AZ)",
      "description": "CpuUtilized from ECS/ContainerInsights, broken down by ContainerName and AvailabilityZone.",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "datasource": { "type": "cloudwatch", "uid": "${cloudwatch_uid}" },
          "dimensions": {
            "ClusterName": "${ecs_cluster_name}",
            "TaskDefinitionFamily": "*",
            "ContainerName": "*",
            "AvailabilityZone": "*"
          },
          "metricName": "CpuUtilized",
          "namespace": "ECS/ContainerInsights",
          "period": "60",
          "region": "default",
          "statistic": "Average",
          "refId": "A",
          "matchExact": false,
          "metricEditorMode": 0,
          "metricQueryType": 0,
          "alias": "{{ContainerName}} / {{AvailabilityZone}}"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Memory Utilized (per container / AZ)",
      "description": "MemoryUtilized from ECS/ContainerInsights, broken down by ContainerName and AvailabilityZone.",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "datasource": { "type": "cloudwatch", "uid": "${cloudwatch_uid}" },
          "dimensions": {
            "ClusterName": "${ecs_cluster_name}",
            "TaskDefinitionFamily": "*",
            "ContainerName": "*",
            "AvailabilityZone": "*"
          },
          "metricName": "MemoryUtilized",
          "namespace": "ECS/ContainerInsights",
          "period": "60",
          "region": "default",
          "statistic": "Average",
          "refId": "A",
          "matchExact": false,
          "metricEditorMode": 0,
          "metricQueryType": 0,
          "alias": "{{ContainerName}} / {{AvailabilityZone}}"
        }
      ]
    }
  ]
}