global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'ecs-services'
    ecs_sd_configs:
      - region: us-east-1
    relabel_configs:
      - source_labels: [__meta_ecs_cluster_name]
        action: keep
        regex: llm-ecs-cluster
